vbell "off"
bind R eval "source $HOME/.screenrc" "echo '.screenrc reloaded!'"
term xterm-256color
startup_message off
source $HOME/.screen-layout
layout save def

bind ^w focus prev
bind ^e focus
bind ^k quit
bind ^l windowlist -b
